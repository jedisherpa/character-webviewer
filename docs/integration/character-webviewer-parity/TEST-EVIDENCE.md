# Test evidence

## Commands

```bash
cd character-webviewer
npm run prove:parity   # exit 0 — 7 contract proofs
npm run build          # exit 0 — Vite production build
```

### prove:parity results (2026-07-22)

- ok — broadcast sources include manual + joe AI/general  
- ok — rantPipeline reuses contract catalog IDs  
- ok — source toggle does not mutate Program  
- ok — Preview load and Take are distinct  
- ok — scenes: 10 voxels + original bull + bowl distinct  
- ok — Wizard Joe Nate profile exact (Ifu36… / eleven_multilingual_v2 / 0.9)  
- ok — bull path clears mass and lands in front  

## Producer versions

| Contract | Producer ref |
|----------|----------------|
| All bird-live.contracts.v1 | RobinSpeech@0f614b4c363af9df45768fc200dcf3c6468ecf22 |

## Screenshots

Browser matrix deferred if dev server not exercised this pass; contracts + build verified. Capture under `docs/integration/character-webviewer-parity/screenshots/` on next interactive pass.
